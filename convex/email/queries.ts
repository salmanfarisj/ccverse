/**
 * Email internal queries placeholder.
 *
 * Note: `getUserByIdQuery` lives at `internal.users.queries.getUserByIdQuery`
 * (convex/users/queries.ts) per the task API path contract. This file exists
 * for any email-specific internal queries that don't belong in the users module.
 */
